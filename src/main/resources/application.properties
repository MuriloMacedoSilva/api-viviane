# --- Configura√ß√£o MySQL ---
quarkus.datasource.db-kind=mysql
quarkus.datasource.username=u437972222_murilo
quarkus.datasource.password=@Afonso38572
# Host corrigido e propriedade de auto-reconex√£o adicionada para mitigar timeouts
#quarkus.datasource.jdbc.url=jdbc:mysql://45.152.46.103/u437972222_banco_viviane?serverTimezone=UTC


# Aumenta o tempo de espera para 60 segundos
quarkus.datasource.jdbc.acquisition-timeout=60
# Garante que o pool n„o fique vazio
quarkus.datasource.jdbc.initial-size=1
quarkus.datasource.jdbc.max-size=5
# Evita erro de conex„o ociosa (comum na Hostinger/Cloud)
#quarkus.datasource.jdbc.background-validation-interval=1M
#quarkus.datasource.jdbc.validation-query-sql=SELECT 1

# Testa a conex„o antes de entregar para o DAO
quarkus.datasource.jdbc.validation-query-sql=SELECT 1
quarkus.datasource.jdbc.background-validation-interval=1M

# Define um tempo m·ximo de vida para a conex„o no pool (menor que o timeout do servidor)
quarkus.datasource.jdbc.max-lifetime=10M

# Adicione autoReconnect, useSSL e o fuso hor·rio
quarkus.datasource.jdbc.url=jdbc:mysql://45.152.46.103/u437972222_banco_viviane?autoReconnect=true&useSSL=false&serverTimezone=UTC

# --- Configura√ß√µes CORS (Para resolver o bloqueio no Frontend) ---
# O erro "Missing map key" pode ser resolvido garantindo que todas as chaves
# de configura√ß√£o CORS tenham um valor expl√≠cito e n√£o sejam separadas por linhas em branco.
# Habilita o CORS globalmente
# Habilita o mecanismo de CORS nativo
# Habilita o CORS nativo
quarkus.http.cors=true

# Permite qualquer origem (ideal para teste, depois vocÍ restringe)
quarkus.http.cors.origins=*

# Define os mÈtodos e headers aceitos
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=Content-Type,Authorization,Accept,Origin

# ForÁa o cabeÁalho mesmo em respostas de erro ou sucesso
quarkus.http.cors.access-control-allow-credentials=false